# 《폐장 후, 우리》 루트 게이트

이 문서는 위험한 3인 이후 자유인 3인으로 넘어가는 정본 분기표다.

## 기본 원칙

- 자유인 3인은 프롤로그에서 기존 게임 파티의 참가 요청을 받아 `$routeUnlocks.freedom`이 열린 경우에만 등장한다.
- 위험한 3인의 연애 관계는 `한 명과의 순애` 또는 `세 명 모두와 합의한 공동 관계`만 성립한다.
- 두 명만 이어지는 관계와 불완전한 연애 지속 엔딩은 없다.
- 관계의 경계를 끝까지 세우지 못하면 중간 상태로 남지 않고 개인 감금 또는 3인 감금 엔딩으로 닫힌다.

## 위험한 3인 종착 상태

| 상태 | 의미 | 자유인 3인 등장 | 자유인 연애 |
|---|---|---:|---:|
| `harem_true` | 위험한 3인 공동 관계 성공 | O | O |
| `solo_true_sera` | 윤새라 순애 성공 | O | X · 친구 고정 |
| `solo_true_yujin` | 강유진 순애 성공 | O | X · 친구 고정 |
| `solo_true_chaerin` | 한채린 순애 성공 | O | X · 친구 고정 |
| `none` | 누구와도 연애하지 않음 | O | O |
| `father_captive` | 새라 사건을 아버지에게 넘기고 은호의 생활까지 다시 관리됨 | X | X |
| `harem_captive` | 위험한 3인의 공동 감금 | X | X |
| `solo_captive_sera` | 윤새라 개인 감금 | X | X |
| `solo_captive_yujin` | 강유진 개인 감금 | X | X |
| `solo_captive_chaerin` | 한채린 개인 감금 | X | X |

## 위험한 3인 관계 선택

- 관계도 80 이상인 인물은 개인 순애 후보가 된다.
- 개인 순애 후보가 여러 명이어도 실제 연애 선택은 한 명만 가능하다.
- 세 사람 모두 관계도 80 이상일 때만 공동 관계 선택지가 추가된다.
- 누구도 80에 도달하지 못하면 `none`으로 끝난다.
- 개인 순애의 건강한 결말은 마지막 선택 하나가 아니라 해당 인물의 경계를 지켜 온 누적 조건을 요구한다. 조건을 놓치면 그 인물의 개인 감금으로 이어진다.
- 공동 관계는 책임과 퇴로를 직접 약속하면 `harem_true`, 결정을 세 사람에게 넘기면 `harem_captive`로 이어진다.

## 자유인 3인 모드

`$routeUnlocks.freedom`이 참일 때만 아래 판정을 수행한다.

- `harem_true`, `none` → `$freedomMode = "romance"`
- `solo_true_*` → `$freedomMode = "friend"`
- `father_captive`, 감금 엔딩 및 그 밖의 상태 → `$freedomMode = "locked"`

순애 이후 자유인 3인은 은호의 게임 친구로 남는다. 기존 연애를 숨긴 채 새 연애를 여는 분기는 만들지 않는다.
